<?php
$this->headTitle($this->translate('Password'));

$this->mainMenu()->setActiveItemId('settings');

$this->pageBreadcrumbs()->setItems([
            $this->translate('Home')=>$this->url('home'),
            $this->translate('Settings')=>$this->url('settings'),
            $this->translate('Password') => $this->url('settings', ['action' => 'password']),
            ]);

$form->get('password')->setAttributes([
    'class'=>'form-control', 
    'placeholder'=>$this->translate('Type password here (6 characters at minimum)')
    ]);

$form->get('submit')->setAttributes(['class'=>'btn btn-primary', 
    'value'=>$this->translate('Submit')]);

$form->prepare();
?>

<h1><?= $this->translate('Settings') ?></h1>

<div class="row">
    <div class="col-md-6">
        <?= $this->form()->openTag($form); ?>
        
        <div class="form-group">
            <?= $this->formLabel($form->get('password')); ?>
            <?= $this->formElement($form->get('password')); ?>
            <?= $this->formElementErrors($form->get('password')); ?>                              
        </div>
        
        <div class="form-group">
        <?= $this->formElement($form->get('submit')); ?>
        </div>
        
        <?= $this->formElement($form->get('csrf')); ?>
        
        <?= $this->form()->closeTag(); ?>
    </div>    
</div>   
